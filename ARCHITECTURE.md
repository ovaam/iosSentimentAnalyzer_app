# Архитектура и принцип работы SentimentAnalyzerSwiftUI

## Обзор приложения

**SentimentAnalyzerSwiftUI** — это iOS-приложение для анализа тональности текста, построенное на SwiftUI и использующее фреймворк NaturalLanguage от Apple. Приложение анализирует введенный пользователем текст и определяет его эмоциональную окраску (позитивную, негативную или нейтральную), а также предоставляет детальную информацию о тексте.

---

## Архитектура приложения

### Структура проекта

```
SentimentAnalyzerSwiftUI/
├── Models/                    # Модели данных и бизнес-логика
│   ├── SentimentAnalyzer.swift    # Основной класс анализатора
│   └── TestAnalysisResult.swift   # Модели данных (Sentiment, TextAnalysisResult)
├── ViewModels/               # ViewModel слой (MVVM)
│   └── AnalysisViewModel.swift   # Управление состоянием анализа
├── Views/                    # UI компоненты
│   ├── ContentView.swift          # Главный экран
│   ├── AnalysisResultView.swift  # Отображение результатов
│   ├── AnalysisDetailView.swift  # Детальная информация
│   ├── ConfidenceIndicator.swift # Индикатор уверенности
│   ├── HistoryView.swift          # История анализов
│   └── TestCasesView.swift        # Тестовые примеры
└── SentimentAnalyzerSwiftUIApp.swift  # Точка входа приложения
```

---

## Принцип работы

### 1. Точка входа приложения

**Файл:** `SentimentAnalyzerSwiftUIApp.swift`

```swift
@main
struct SentimentAnalyzerSwiftUIApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}
```

Приложение использует SwiftUI App Lifecycle. При запуске создается главное окно с `ContentView`.

---

### 2. Главный экран (ContentView)

**Файл:** `Views/ContentView.swift`

**Компоненты:**
- **TextEditorView** — поле ввода текста с подсчетом символов
- **RealTimeAnalysisView** — анализ в реальном времени при вводе
- **AnalysisButton** — кнопка запуска полного анализа
- **AnalysisResultsView** — отображение результатов
- **TestCasesView** — быстрые тестовые примеры
- **AnalysisDetailsView** — детальная информация (раскрывается по кнопке)

**Принцип работы:**
1. Пользователь вводит текст в `TextEditor`
2. При изменении текста срабатывает `RealTimeAnalysisView`, который выполняет быстрый предварительный анализ
3. При нажатии кнопки "Анализировать тональность" запускается полный анализ через `AnalysisViewModel`

---

### 3. ViewModel слой (MVVM паттерн)

**Файл:** `ViewModels/AnalysisViewModel.swift`

```swift
@MainActor
class AnalysisViewModel: ObservableObject {
    @Published var result: TextAnalysisResult?
    @Published var isAnalyzing = false
    @Published var analysisDetails: [TextAnalysisResult.AnalysisDetail] = []
    @Published var errorMessage: String?
    private let analyzer = SentimentAnalyzer()
}
```

**Принцип работы:**
- Использует `@MainActor` для работы в главном потоке
- `@Published` свойства автоматически обновляют UI при изменении
- Метод `analyzeText(_:)` запускает асинхронный анализ в `Task`
- Результаты сохраняются в `result` и `analysisDetails`
- Ошибки обрабатываются и сохраняются в `errorMessage`

**Процесс анализа:**
1. Проверка, что текст не пустой
2. Установка `isAnalyzing = true` (показывает индикатор загрузки)
3. Асинхронный вызов `analyzer.analyze(text)`
4. Сохранение результата или ошибки
5. Установка `isAnalyzing = false`

---

### 4. Ядро анализатора (SentimentAnalyzer)

**Файл:** `Models/SentimentAnalyzer.swift`

Это основной класс, который выполняет весь NLP-анализ текста.

#### 4.1. Главный метод `analyze(_:)`

Выполняет комплексный анализ текста в несколько этапов:

```swift
func analyze(_ text: String) async throws -> TextAnalysisResult
```

**Этапы анализа:**

1. **Определение языка** (`detectLanguage`)
   - Использует `NLLanguageRecognizer` из NaturalLanguage
   - Определяет доминирующий язык текста
   - Возвращает код языка (например, "ru", "en")

2. **Токенизация и статистика** (`tokenize`)
   - Использует `NLTagger` с схемой `.tokenType`
   - Подсчитывает количество слов (unit: `.word`)
   - Подсчитывает количество предложений (unit: `.sentence`)
   - Игнорирует пунктуацию и пробелы при подсчете слов

3. **Анализ тональности** (`analyzeSentiment`)
   - **Первичный метод:** Использует встроенный анализатор Apple
     - `NLTagger` с схемой `.sentimentScore`
     - Возвращает числовой score от -1.0 до 1.0
     - Интерпретация:
       - `≥ 0.3` → Позитивный
       - `-0.3 до 0.3` → Нейтральный
       - `< -0.3` → Негативный
   - **Fallback метод:** Если встроенный не сработал
     - Анализ на основе ключевых слов
     - Подсчет положительных и отрицательных слов
     - Определение тональности по соотношению

4. **Анализ частей речи** (`analyzePartsOfSpeech`)
   - Использует `NLTagger` с схемой `.lexicalClass`
   - Определяет части речи каждого слова (существительное, глагол, прилагательное и т.д.)
   - Подсчитывает количество каждой части речи
   - Создает детали анализа для каждой части речи

5. **Поиск именованных сущностей** (`findNamedEntities`)
   - Использует `NLTagger` с схемой `.nameType`
   - Находит имена людей, организаций, мест и т.д.
   - Использует опцию `.joinNames` для объединения составных имен
   - Возвращает массив найденных сущностей с их типами

6. **Проверка на токсичность** (`checkToxicity`)
   - Простая проверка по списку токсичных слов
   - В реальном приложении следует использовать ML-модель
   - Если найдены токсичные слова, добавляется предупреждение

#### 4.2. Вспомогательные методы

**`detectLanguage(_:)`**
```swift
private func detectLanguage(_ text: String) async throws -> String
```
- Создает `NLLanguageRecognizer`
- Обрабатывает строку через `processString(_:)`
- Возвращает `dominantLanguage.rawValue` или "Не определен"

**`tokenize(_:)`**
```swift
private func tokenize(_ text: String) async throws -> (wordCount: Int, sentenceCount: Int)
```
- Использует `enumerateTags` для перебора слов и предложений
- Возвращает кортеж с количеством слов и предложений

**`analyzeSentiment(_:)`**
```swift
private func analyzeSentiment(_ text: String) async throws -> (Sentiment, Double)
```
- Пытается использовать встроенный анализатор Apple
- Если не получается, вызывает `analyzeWithCustomModel`
- Возвращает кортеж (Sentiment, confidence)

**`analyzeWithCustomModel(_:)`** (Fallback)
```swift
private func analyzeWithCustomModel(_ text: String) async throws -> (Sentiment, Double)
```
- Анализирует текст на основе ключевых слов
- Подсчитывает положительные и отрицательные слова
- Определяет тональность по соотношению
- Вычисляет уверенность на основе найденных слов

**`analyzePartsOfSpeech(_:)`**
```swift
private func analyzePartsOfSpeech(_ text: String) async throws -> [TextAnalysisResult.AnalysisDetail]
```
- Перебирает все слова с их частями речи
- Группирует по типам частей речи
- Создает массив деталей анализа

**`findNamedEntities(_:)`**
```swift
private func findNamedEntities(_ text: String) async throws -> [String]
```
- Находит именованные сущности в тексте
- Форматирует как "Сущность (Тип)"
- Возвращает массив строк

**`checkToxicity(_:)`**
```swift
private func checkToxicity(_ text: String) async throws -> Bool
```
- Проверяет наличие токсичных слов в тексте
- Возвращает `true`, если найдены токсичные паттерны

---

### 5. Модели данных

**Файл:** `Models/TestAnalysisResult.swift`

#### 5.1. Enum `Sentiment`

```swift
enum Sentiment: String, Codable {
    case positive = "Позитивный"
    case negative = "Негативный"
    case neutral = "Нейтральный"
}
```

**Свойства:**
- `color: Color` — цвет для отображения (зеленый/красный/серый)
- `emoji: String` — эмодзи для визуализации (😊/😠/😐)

#### 5.2. Struct `TextAnalysisResult`

```swift
struct TextAnalysisResult: Codable {
    let text: String
    let sentiment: Sentiment
    let confidence: Double
    let language: String
    let wordCount: Int
    let entities: [String]
    let details: [AnalysisDetail]
    let timestamp: Date
}
```

**Свойства:**
- `text` — исходный текст
- `sentiment` — определенная тональность
- `confidence` — уверенность модели (0.0-1.0)
- `language` — определенный язык
- `wordCount` — количество слов
- `entities` — найденные именованные сущности
- `details` — массив деталей анализа
- `timestamp` — время анализа

**Вложенная структура `AnalysisDetail`:**
```swift
struct AnalysisDetail: Codable {
    let title: String
    let value: String
    let type: DetailType
    
    enum DetailType: String, Codable {
        case info, warning, success, error
    }
}
```

---

### 6. UI компоненты

#### 6.1. AnalysisResultsView

**Файл:** `Views/AnalysisResultView.swift`

**Компоненты:**
- **SentimentCard** — карточка с основным результатом
  - Отображает тональность с эмодзи и цветом
  - Показывает уверенность в процентах
  - Отображает определенный язык
- **ConfidenceIndicator** — визуальный индикатор уверенности
  - Прогресс-бар с цветовой индикацией
  - Зеленый (≥80%), Желтый (50-80%), Оранжевый (<50%)
- **AnalysisDetailRow** — строка детали анализа
  - Иконка в зависимости от типа (info/warning/success/error)
  - Заголовок и значение

#### 6.2. ConfidenceIndicator

**Файл:** `Views/ConfidenceIndicator.swift`

**Принцип работы:**
- Использует `GeometryReader` для адаптивной ширины
- Рисует фоновый прямоугольник (серая линия)
- Рисует индикатор уверенности (цветной прямоугольник)
- Добавляет круг на текущей позиции
- Показывает значения 0%, текущее значение, 100%

#### 6.3. RealTimeAnalysisView

**Файл:** `Views/ContentView.swift` (встроенный компонент)

**Принцип работы:**
- Отслеживает изменения текста через `onChange(of: text)`
- Запускает быстрый анализ с задержкой 0.5 секунды (debounce)
- Использует простой алгоритм на основе ключевых слов
- Показывает результат только если текст > 10 символов

**Алгоритм быстрого анализа:**
1. Список положительных и отрицательных слов
2. Подсчет слов в тексте
3. Определение тональности по балансу

#### 6.4. HistoryView

**Файл:** `Views/HistoryView.swift`

**Принцип работы:**
- Использует `@AppStorage` для сохранения истории
- Сохраняет массив `TextAnalysisResult` в формате JSON
- При загрузке декодирует данные и сортирует по времени
- Поддерживает удаление элементов (swipe to delete)

**Сохранение:**
```swift
@AppStorage("analysisHistory") private var historyData: Data = Data()
```

**Загрузка:**
```swift
if let decoded = try? JSONDecoder().decode([TextAnalysisResult].self, from: historyData) {
    history = decoded.sorted { $0.timestamp > $1.timestamp }
}
```

---

## Поток данных

### Полный цикл анализа:

```
1. Пользователь вводит текст
   ↓
2. RealTimeAnalysisView выполняет быстрый анализ (опционально)
   ↓
3. Пользователь нажимает "Анализировать тональность"
   ↓
4. AnalysisViewModel.analyzeText(_:) вызывается
   ↓
5. SentimentAnalyzer.analyze(_:) запускается асинхронно
   ↓
6. Выполняются все этапы анализа:
   - Определение языка
   - Токенизация
   - Анализ тональности
   - Анализ частей речи
   - Поиск сущностей
   - Проверка токсичности
   ↓
7. Создается TextAnalysisResult
   ↓
8. Результат сохраняется в AnalysisViewModel.result
   ↓
9. UI автоматически обновляется через @Published
   ↓
10. AnalysisResultsView отображает результат
```

---

## Используемые технологии

### Фреймворки Apple:

1. **NaturalLanguage** — основной фреймворк для NLP
   - `NLLanguageRecognizer` — определение языка
   - `NLTagger` — токенизация, анализ тональности, части речи, именованные сущности
   - `NLModel` — работа с ML-моделями (не используется в текущей версии)

2. **SwiftUI** — UI фреймворк
   - `@State`, `@StateObject`, `@ObservedObject` — управление состоянием
   - `@Published` — реактивное обновление UI
   - `Task`, `async/await` — асинхронность

3. **Foundation**
   - `Codable` — сериализация данных
   - `Date` — работа с датами

4. **CoreML** (импортирован, но не используется)
   - Предназначен для работы с ML-моделями
   - В текущей версии используется fallback на ключевые слова

---

## Особенности реализации

### 1. Асинхронность

Все методы анализа помечены как `async throws`, что позволяет:
- Не блокировать UI поток
- Обрабатывать ошибки через `try-catch`
- Использовать `await` для ожидания результатов

### 2. Обработка ошибок

```swift
enum AnalysisError: Error {
    case modelNotFound
    case invalidText
    case analysisFailed
}
```

Ошибки обрабатываются в `AnalysisViewModel` и отображаются пользователю.

### 3. Реактивность

Использование `@Published` и `ObservableObject` обеспечивает автоматическое обновление UI при изменении данных.

### 4. Debouncing

В `RealTimeAnalysisView` используется задержка 0.5 секунды перед анализом, чтобы не анализировать текст при каждом нажатии клавиши.

---

## Возможные улучшения

1. **Добавление CoreML модели**
   - Создать или загрузить обученную модель для анализа тональности
   - Заменить fallback метод на реальную ML-модель

2. **Кэширование результатов**
   - Сохранять результаты анализа для одинаковых текстов
   - Ускорить повторный анализ

3. **Экспорт результатов**
   - Возможность экспортировать историю анализов
   - Поделиться результатами

4. **Расширенный анализ**
   - Анализ эмоций (радость, грусть, гнев и т.д.)
   - Определение тематики текста
   - Анализ сарказма

5. **Оптимизация производительности**
   - Кэширование результатов токенизации
   - Параллельная обработка разных этапов анализа

---

## Заключение

Приложение использует современные подходы iOS-разработки:
- **MVVM архитектура** для разделения логики и UI
- **SwiftUI** для декларативного UI
- **NaturalLanguage** для NLP-анализа
- **async/await** для асинхронных операций
- **Codable** для сериализации данных

Все компоненты работают вместе, обеспечивая плавный пользовательский опыт и точный анализ тональности текста.


